/*import {
    Column,
    Entity,
    PrimaryGeneratedColumn,
} from 'typeorm';

@Entity({
    name: 'question',
})
export class Question {
    @PrimaryGeneratedColumn('uuid')
    question_id: string;

    @Column({
        type: 'varchar',
        length: 255,
    })
    question_name: string;

    @Column({
        type: 'uuid'
    })
    poll_id: string;
}*/
